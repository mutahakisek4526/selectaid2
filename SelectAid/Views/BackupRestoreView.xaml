<UserControl x:Class="SelectAid.Views.BackupRestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SelectAid.Views">
    <StackPanel Margin="24">
        <Button Content="Create Backup" Style="{StaticResource PrimaryButton}" Margin="6" Height="80" Click="OnCreate" />
        <ListBox ItemsSource="{Binding Backups}" Height="200" FontSize="28" x:Name="BackupList" />
        <local:LongPressButton Content="Restore Selected" Style="{StaticResource PrimaryButton}" Margin="6" Height="80" Click="OnRestoreClick" LongPressed="OnRestoreLongPress" />
        <TextBlock Text="{Binding Status}" FontSize="28" Margin="0,12,0,0" />
    </StackPanel>
</UserControl>
