<UserControl x:Class="SelectAid.Views.BackupRestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="24">
        <Button Content="Create Backup" Style="{StaticResource PrimaryButton}" Margin="6" Height="80" Click="OnCreate" />
        <ListBox ItemsSource="{Binding Backups}" Height="200" FontSize="20" x:Name="BackupList" />
        <Button Content="Restore Selected" Style="{StaticResource PrimaryButton}" Margin="6" Height="80" Click="OnRestore" />
        <TextBlock Text="{Binding Status}" FontSize="20" Margin="0,12,0,0" />
    </StackPanel>
</UserControl>
